# Nixpacks configuration for Operabase Railway deployment

[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build:railway"]

[start]
cmd = "npm run start:railway"

[variables]
NODE_ENV = "production"
PORT = "3000" 